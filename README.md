# bardi

The framework-independent prototype resolves immutable Procedure Versions by evaluation date, keeps publication state separate from claim-level evidence trust, preserves established stale values only as dated historical context, and keeps disputed or unverified guidance locally inconclusive without dropping reliable content.

Run `python -m unittest discover -s prototype/tests -v` from the repository root.
