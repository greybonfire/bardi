# bardi

The framework-independent prototype resolves immutable Procedure Versions by evaluation date, keeps publication state separate from claim-level evidence trust, and preserves stale or disputed guidance as dated context without dropping reliable content.

Run `python -m unittest discover -s prototype/tests -v` from the repository root.
